<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dev Client</title>
  <style>
    body {
      font-family: system-ui;
      max-width: 680px;
      margin: 48px auto;
      padding: 0 20px;
    }
    .nav-links a {
      margin-right: 10px;
      color: #0066cc;
      text-decoration: none;
    }
    .nav-links a:hover {
      text-decoration: underline;
    }
    .status {
      padding: 10px;
      border-radius: 4px;
      margin: 20px 0;
    }
    .signed-in {
      background-color: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
    }
    .signed-out {
      background-color: #f8d7da;
      color: #721c24;
      border: 1px solid #f1b0b7;
    }
  </style>
</head>
<body>
  <h2>Dev Client</h2>
  <div class="status <%= tokens ? 'signed-in' : 'signed-out' %>">
    <%= tokens ? 'Signed in' : 'Signed out' %>
  </div>
  <div class="nav-links">
    <a href="/login">Login</a>
    <a href="/me">Me</a>
    <a href="/accounts">Call API</a>
    <a href="/logout">Logout</a>
  </div>
</body>
</html>