name: Deploy App to VM
on:
  push:
    paths:
      - 'apps/app/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'apps/app/package-lock.json'

      - name: Build
        working-directory: apps/app
        run: |
          npm ci
          npm run build || true

      - name: Rsync to VM
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -az --delete --exclude=node_modules
          path: apps/app/
          remote_path: /srv/apps/app/
          remote_host: ${{ secrets.VM_HOST }}
          remote_user: deploy
          remote_key: ${{ secrets.VM_SSH_KEY }}

      - name: Install prod deps & restart
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: deploy
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            cd /srv/apps/app
            npm ci --omit=dev
            sudo systemctl restart plaidypus-app
