# OIDC + Express Monorepo - Production Configuration Template
# CRITICAL: Never commit this file with real secrets to version control

# ===== SERVICE URLS =====
# Update these with your production domain names
OP_ISSUER=https://auth.yourdomain.com
APP_HOST=https://app.yourdomain.com
APP_BASE_URL=https://app.yourdomain.com
API_HOST=https://api.yourdomain.com
API_BASE_URL=https://api.yourdomain.com

# ===== SERVICE PORTS =====
# Use standard ports for production (80/443 handled by reverse proxy)
OP_PORT=3001
APP_PORT=3004
API_PORT=3003

# ===== OAUTH CONFIGURATION =====
CLIENT_ID=your-production-client-id
# CRITICAL: Generate secure secrets with: openssl rand -hex 32
CLIENT_SECRET=REPLACE_WITH_SECURE_64_CHAR_HEX_STRING

# Callback URL for OAuth flow (must match your production domain)
REDIRECT_URI=https://app.yourdomain.com/callback

# ===== SECURITY SECRETS =====
# CRITICAL: Generate secure cookie secret with: openssl rand -hex 32
COOKIE_SECRET=REPLACE_WITH_SECURE_64_CHAR_HEX_STRING

# ===== API CONFIGURATION =====
# Update with your production API audience
API_AUDIENCE=api://your-production-api

# ===== ENVIRONMENT =====
NODE_ENV=production

# ===== PRODUCTION SETTINGS =====
# Disable debug logging in production
DEBUG=false

# ===== DATABASE (when implemented) =====
# DATABASE_URL=postgresql://user:password@host:port/database
# DATABASE_SSL=true

# ===== SECURITY HEADERS =====
# These will be handled by helmet.js middleware
# Additional security can be added via reverse proxy (nginx/cloudflare)

# ===== MONITORING (optional) =====
# LOG_LEVEL=info
# METRICS_ENABLED=true
# HEALTH_CHECK_TOKEN=REPLACE_WITH_SECURE_TOKEN